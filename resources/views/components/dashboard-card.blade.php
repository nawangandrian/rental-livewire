<div class="p-4 bg-white dark:bg-gray-800 rounded-xl shadow flex items-center gap-4 border border-gray-200 dark:border-gray-700">
    <div class="text-3xl text-{{ $color }}-500">
        <i class="fas fa-{{ $icon }}"></i>
    </div>
    <div>
        <div class="text-gray-500 dark:text-gray-400 text-sm">{{ $title }}</div>
        <div class="text-gray-900 dark:text-gray-100 font-bold text-xl">{{ $value }}</div>
    </div>
</div>
